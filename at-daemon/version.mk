# Local version file for the vendored ubus-at-daemon package.
#
# Upstream (QModem) keeps this in the feed root; we carry our own copy so that
# this directory is self-contained.
QMODEM_VERSION:=2026.09.18-vendored
QMODEM_RELEASE:=1
